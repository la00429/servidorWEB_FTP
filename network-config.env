# ===========================================
# CONFIGURACIÓN DE RED PARA DHCP/DNS
# ===========================================
# Configuración específica para tu red

# Información del servidor
HOSTNAME=webserver-ftp
DOMAIN=local

# Configuración DNS/DHCP (ambos en la misma IP)
DNS_PRIMARY=192.168.1.2        # Servidor DNS/DHCP principal
DNS_SECONDARY=8.8.8.8          # DNS secundario (fallback)
DNS_SEARCH_DOMAIN=local        # Dominio de búsqueda

# IPs de servidores de red
DNS_SERVER_IP=192.168.1.2      # IP del servidor DNS
DHCP_SERVER_IP=192.168.1.2     # IP del servidor DHCP (misma IP)

# Información de red configurada
SERVER_IP_CURRENT=10.4.72.103  # IP actual del servidor Ubuntu
SERVER_IP_TARGET=192.168.1.3   # IP objetivo después de DHCP
SERVER_IP=192.168.1.3           # IP del servidor de archivos
NETWORK_SUBNET=192.168.1.0/24  # Subred objetivo

# Configuración de red Docker
DOCKER_SUBNET=172.20.0.0/16    # Subred para el contenedor
DOCKER_GATEWAY=172.20.0.1      # Gateway del contenedor
CONTAINER_IP=172.20.0.10       # IP fija del contenedor (opcional)

# Configuración FTP
FTP_USER=ftpuser
FTP_PASS=ftppass123
FTP_PASV_ADDRESS=192.168.1.3   # IP para modo pasivo FTP

# Configuración adicional
NETWORK_NAME=webnet-bridge     # Nombre del bridge de red
